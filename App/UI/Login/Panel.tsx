export default function Panel() {
    const styles = {
        panel: {
            padding: "15px",
            background: "rgba(0, 0, 0, 0.5)"
        },
    }

    return (
        <div
            className="panel" 
            style={styles.panel}>
            
        </div>
    );
}